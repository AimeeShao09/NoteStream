# Optional: path for local sqlite cache file
NOTESTREAM_DB_PATH=.cache/notestream.db

# Optional: default model when request body doesn't provide it
NOTESTREAM_DEFAULT_MODEL=qwen3.5-plus

# Optional: backend CORS origin for production
NOTESTREAM_ALLOWED_ORIGINS=http://localhost:5173
